-- lsp/vtsls.lua
-- Config moved to lua/lsp/init.lua via vim.lsp.config() to ensure it takes
-- priority over nvim-lspconfig defaults (which would otherwise overwrite
-- filetypes and remove "vue").
return {}
