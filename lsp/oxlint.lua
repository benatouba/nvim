-- lsp/oxlint.lua
return {
  -- oxlint's default lspconfig already includes "vue" in its filetypes.
  -- You can leave this empty to use defaults, or explicitly ensure single file support:
  single_file_support = true,
}
